# KLQ 编程机器人功能需求文档

版本：V0.2（根据用户反馈修订）  
日期：2026-09-14  
状态：整体功能仍待用户修改确认；用户另行授权的最小 USB/TM1640 引导已进入开发与实机联调，见 README_BOOTLOADER.md。

## 1. 产品目标与范围

KLQ 是一款支持图形化编程的机器人控制器。用户在上位机中通过类似乐高 SPIKE 的积木拖拽方式编写程序，上位机生成 Python 代码，通过蓝牙或 USB 下载到 KLQ。KLQ 在本机解释执行程序，读取传感器并控制外接电机等设备。

本文件先确定机器人端“需要实现什么”和基本操作流程。上位机已有，本阶段不讨论或开发上位机功能。Python 解释器、固件架构及资源使用方案在功能确认后另行设计，通信协议和 Python API 后续与现有上位机对接。

文中“已确定”来自用户说明、确认反馈或现有 IO 表；“建议”是本次提出的方案；“待确认”表示尚不能作为最终开发要求。明确标注为已确定的功能按用户反馈执行，其余建议继续等待审阅。

## 2. 已确定的基础条件

| 项目 | 当前信息 |
| --- | --- |
| 产品名称 | KLQ |
| 主控 MCU | 按用户说明采用 APM32E103RET6 |
| 编程方式 | 上位机图形化拖拽，生成 Python 代码 |
| 执行位置 | 程序下载到机器人，由本机精简 Python 解释器执行 |
| 下载连接 | 蓝牙、USB |
| 蓝牙接口 | 串口 5 连接 ECB02C，与现有规格书型号一致，已确认 |
| 外接接口 | 串口 1、2、3，用于外部传感器或电机 |
| 电源保持 | 上电后尽早将 PC5 配置为高电平输出，维持开机 |
| 关机控制 | PC5 输出低电平，释放电源保持并断电 |
| 电源键 PC4 | 长按开机／关机；短按运行／停止／菜单确认，已确定 |
| 用户按键 PC3 | 专供用户编程使用，已确定 |
| SC7A20HTR | 数据提供给用户程序使用，已确定 |
| 外部 Flash | GD25Q80ESIG 暂时备用；是否掉电保存程序后续决定 |
| 上位机 | 已有，本阶段不考虑上位机开发 |
| 供电与采样 | IO 表注明单节锂电池，PA0 采集 1/2 分压后的电池电压 |
| 其他板载资源 | 两个按键、TM1640、SC7A20HTR、GD25Q80ESIG、外部 16 MHz 晶振 |

型号已确认：`IO.xlsx` 的 A1 中 `APM32E103R3T6` 为表头笔误，实际 MCU 为 `APM32E103RET6`；蓝牙实际型号为 `ECB02C`，采用现有《ECB02C 蓝牙芯片技术手册 V1.0》。本次仅修订功能文档，原始 IO 表未修改。

## 3. 典型使用流程

1. 用户长按电源键开机，KLQ 建立电源保持，初始化外设并进入待机状态。
2. 用户打开上位机，新建或打开图形化项目。
3. 拖拽积木完成逻辑，查看生成的 Python 代码。
4. 通过 USB 或蓝牙连接 KLQ，上位机读取设备信息与运行状态。
5. 点击下载，将程序传入机器人；机器人校验完整性并将程序置为可运行，反馈成功或失败原因。本阶段不要求写入外部 Flash。
6. 通过上位机运行命令，或在待机状态短按电源键，启动本次上电期间已下载的有效程序。
7. 机器人执行程序，通过上位机显示日志、状态或错误信息。
8. 运行中短按电源键停止程序，或长按关机；停止时向外接电机发送停止命令，关机时完成必要清理后拉低 PC5。菜单中短按电源键用于确认当前选项。

建议支持下载后的脱机运行：本次上电期间，上位机断开后，已启动的本地程序继续执行。脱机执行与掉电保存是两个独立功能；掉电后是否保留程序尚未决定，当前不承诺断电重启后仍可运行原程序。

## 4. 与现有上位机的对接范围

上位机已有，本阶段仅保留机器人端需要对接的能力，不增加上位机编辑器、项目管理或界面开发要求。以下对接细节在后续取得现有协议与生成代码样例后确定。

| 对接能力 | 机器人端建议行为 |
| --- | --- |
| 设备连接 | 经 USB 或蓝牙接收请求，返回设备信息和连接就绪状态 |
| 程序下载 | 接收 Python 程序，检查长度和完整性，返回下载结果 |
| 执行控制 | 接收运行、停止命令，返回执行状态 |
| 状态与日志 | 返回运行状态、电池电压、程序输出和可获得的错误行号 |
| Python API | 提供用户按键、SC7A20HTR 数据、显示及外接设备的调用接口 |

解释器支持的语法与 API 应结合已有上位机生成的代码确认。

## 5. 机器人功能

### 5.1 开关机与电池管理

- 启动的早期阶段建立 PC5 高电平保持，不能等待解释器、蓝牙或显示初始化完成后才保持电源。允许的启动时间窗口以电源电路实测为准。
- 初始化期间不主动启动电机。发生可恢复的初始化故障时显示或上报错误，保留重新下载和恢复能力。
- 已确定电源键长按开机、长按关机，具体时长待定。断电时的按键唤醒需由电源硬件配合，固件在 MCU 获得供电后完成按键判定与电源保持；具体时序后续结合电路确认。
- 开机按压释放前不将同一次按压识别为关机或短按运行。长按触发后释放也不再生成短按事件。
- 已确定电源键短按用于运行、停止及菜单确认，按当前状态分配动作。关机流程不依赖用户 Python 程序正常返回。
- 关机时先终止用户程序、向电机发送停止命令、完成必要清理，再拉低 PC5。本阶段不增加程序写入 Flash 的关机步骤。等待过程设置上限，避免失联外设导致无法关机。
- 定期采样电池电压并滤波，根据实际分压与 ADC 参考电压换算。初版优先提供电压值，电量百分比需经电池曲线标定后再决定。
- 建议支持低电压提示及严重低电压停机；阈值、延时和恢复条件待电池与电源设计确认。
- USB 插入检测与 USB 数据连接状态分别处理。仅检测到 USB 电压不代表上位机已连接，也不能直接判断充电中或充满。
- 插入 USB 时 PC5 拉低后是否整机断电，必须结合电源路径确认。

### 5.2 Python 程序执行

- 本机支持一个用户程序运行，提供开始、停止、结束和错误状态。
- 初版目标包括变量、基本数值与布尔运算、条件判断、循环、函数、字符串，以及受容量限制的容器。最终语法范围由解释器验证后确定。
- 提供统一的 KLQ 硬件接口，让上位机生成代码不直接操作 MCU 引脚。
- 已确定用户程序可读取用户按键 PC3 和内置 SC7A20HTR 数据；另建议支持读取外接设备、控制显示和电机，并输出调试信息。电源键 PC4 由系统处理。
- 等待、循环和外设调用期间仍须处理停止请求、通信及电源键。外设等待必须有超时。
- 语法错误、运行异常、内存不足或外设超时应报告明确原因；程序异常退出后向电机发送停止命令。
- 用户程序结束或被停止后，建议所有电机停止，返回待机；显示是否保留最后画面待确认。
- 不向普通用户程序开放任意存储器访问、固件擦写或 PC5 直接控制。
- 建议保留故障恢复能力：即使当前下载的程序异常，也能停止执行并重新接收程序。

解释器目前不选定。MicroPython 或其他精简实现均作为候选，后续先验证目标芯片上的移植条件、内部 Flash/RAM 占用、可用堆空间、执行效率和停止响应，再确定方案。外部 Flash 可保存文件，但不能直接等同于解释器可用 RAM。不预先承诺完整桌面 Python 或第三方包兼容性。

### 5.3 程序下载与临时驻留

- 已确定外部 GD25Q80ESIG 暂时备用，当前不安排用户程序存储、文件系统或参数持久化功能。
- 是否需要掉电保存程序后续再决定，当前不将持久化及断电恢复作为必需功能。
- 初版建议在本次上电期间维护一个当前程序，记录名称、长度、校验值及有效状态。临时驻留方式和最大容量在解释器及 RAM 资源评估后确定，不以外部 Flash 作为默认依赖。
- 下载采用分包、序号、确认、超时与重试机制，处理 USB/蓝牙的拆包和合包。
- 下载完成后校验整个文件，仅将完整且校验通过的程序标记为可运行。
- 下载中断后不得运行残缺程序，允许重新下载。替换程序时是否能保留旧程序由可用 RAM 决定，不预设双份缓存。
- 正在运行时请求下载，建议先停止当前程序再开始传输。
- 断电重启后的程序保留行为待定。当前验收不要求保留；无有效程序时短按运行应提示需要下载。
- 程序大小和日志缓存容量在资源评估后确定，不在初稿中填入未经验证的数值。

### 5.4 USB 与蓝牙通信

USB 和蓝牙使用相同的应用层功能，包括设备信息查询、程序下载、运行、停止、状态读取和日志返回。物理连接建立后还需完成应用层握手，才显示“设备就绪”。

USB 建议优先评估虚拟串口方式，具体设备类型、驱动要求及目标系统兼容性后续确认。

蓝牙经串口 5 与 MCU 通信，PA8 用于连接状态检测。依据现有 ECB02C 手册，初步设计参考如下，均需与实装芯片验证：

- KLQ 作为 BLE 从机，使用可区分多台机器的名称，例如 `KLQ-xxxx`。
- 串口先以手册默认的 115200、8 数据位、无校验、1 停止位作为联调参数。
- 上位机应完成相应特性的通知订阅，再验证双向通信；PA8 有连接信号不等于应用层可收发。
- 按实际协商的 MTU 分包并控制发送节奏，不假定一次发送等于一个完整应用报文。
- AT 配置、连接提示文本与用户数据需明确隔离，避免源码或日志被解释成配置命令。
- 核实蓝牙 SLEEP、AT_EN、ROLE 等管脚实际接法，确保正常下载期间能够接收串口数据。

现有 V1.0 手册对 Windows/Linux 的说明提到 ECB02U 适配器。目标平台能否直接使用内置蓝牙，留待实测，不据此假定所有电脑都能直接连接。

两种通道同时连接时，建议只允许一个通道持有下载和运行控制权；其他通道返回忙状态，停止请求优先处理。控制权释放和切换规则在协议设计中明确。

### 5.5 外接传感器与电机

- 暂将串口 1、2、3 分别命名为外接端口 1、2、3，最终以外壳标识确认。
- 三个端口独立通信，某个端口超时不应阻塞其他端口或开关机功能。
- 提供读取传感器、控制电机方向及输出、停止电机等基础能力，具体功能以实际外设支持为准。
- 自动识别设备、热插拔、位置或速度闭环控制，都需要外设协议支持，暂不作为已具备能力。
- 未连接、设备类型不符、超时或数据异常时，返回明确错误。
- 上位机用名称或端口编号选择设备，不要求用户理解具体 UART 引脚。
- 外设接口电压、供电能力、连接器定义、波特率、帧格式及设备型号尚未给出，需要在驱动开发前补齐。

通过串口发送停止命令不能保证失联电机已经停转。后续需确认电机端是否具备通信超时停机功能，并确定响应时间要求。

### 5.6 板载按键、显示与传感器

| 资源 | 功能与确认状态 | 待确认事项 |
| --- | --- | --- |
| 按键 1／电源键（PC4） | 已确定：长按开机／关机，短按运行／停止／菜单确认 | 有效电平、长按时长、菜单内容与导航方式 |
| 按键 2／用户按键（PC3） | 已确定：给用户编程使用，不分配系统运行／停止或菜单操作 | API 返回形式；建议提供按下状态及按下／释放事件 |
| TM1640 显示接口 | 待机、连接、运行、低电量、错误提示；用户图案显示和清屏 | 实际显示器类型、尺寸、映射、亮度能力 |
| SC7A20HTR | 已确定：通过 Python API 向用户程序提供传感器数据；倾斜及摇晃等派生功能待评估 | 器件资料、数据单位、安装方向、量程、采样率及校准方式 |

目前未提供显示模组和传感器完整资料，不预设点阵尺寸，不承诺陀螺仪或绝对方向功能。系统错误和关机提示建议优先于用户显示内容。

电源键短按的建议状态规则：待机且有有效程序时启动；运行中停止；菜单中确认当前选项。菜单内容、进入／退出和选项切换方式待确认，不借用用户按键作为默认导航键。运行时建议优先处理停止，不进入菜单而改变短按含义。

### 5.7 状态与故障处理

建议运行状态包括：初始化、待机、下载中、运行中、程序错误、关机中。USB 与蓝牙连接状态单独记录，可与这些状态同时存在。

| 场景 | 建议行为 |
| --- | --- |
| 下载时断线 | 中止或超时退出本次下载，不激活残缺文件，可重新下载 |
| 脱机程序运行时断线 | 本地程序继续运行，本机停止操作仍有效 |
| Python 异常或内存不足 | 结束程序，向电机发送停止命令，记录并显示错误 |
| 传感器通信超时 | 有界等待后返回错误，由已确认的程序错误策略处理 |
| 用户死循环 | 系统仍能响应停止和长按关机，不依赖用户代码主动配合 |
| 固件失去响应 | 建议看门狗恢复；复位后不自动重启故障程序，电源是否保持需硬件验证 |
| 无有效用户程序 | 保持待机，提示需要下载程序 |

在线遥控如果后续加入，应另设断线停机规则，不能直接沿用脱机程序继续执行的策略。

## 6. IO 分配汇总

来源：`IO.xlsx`，A2:B33。表中 TX/RX 按 MCU 侧信号命名。

| 引脚 | 分配 |
| --- | --- |
| PA0 | 电池 ADC，单节锂电池，1/2 分压 |
| PA2 / PA3 | TX2 / RX2，外接端口 2（编号暂定） |
| PA8 | BLE 连接状态检测 |
| PA9 / PA10 | TX1 / RX1，外接端口 1（编号暂定） |
| PA11 / PA12 | USB D- / D+ |
| PA13 / PA14 | SWDIO / SWCLK |
| PC3 | 按键 2，供用户编程使用 |
| PC4 | 按键 1／电源键 |
| PC5 | 电源维持，高电平保持、低电平关机 |
| PC10 / PC11 | TX4 / RX4，用途待定 |
| PC12 / PD2 | TX5 / RX5，连接 ECB02C（用户已确认型号） |
| PB3 / PB5 | SC7A20HTR INT1 / INT2 |
| PB4 | VUSB_IN，USB 5V 输入检测，表内标记备用 |
| PB6 / PB7 | TM1640 SCL / SDA，时序按器件协议确认 |
| PB8 / PB9 | I2C SCL / SDA，连接 SC7A20HTR |
| PB10 / PB11 | TX3 / RX3，外接端口 3（编号暂定） |
| PB12 / PB13 / PB14 / PB15 | GD25Q80ESIG SPI NSS / SCK / MISO / MOSI，Flash 暂时备用 |
| PD0 / PD1 | 外部 16 MHz 晶振 |

后续硬件核对重点：引脚复用和调试接口冲突、PB4 的 USB 检测电平及输入耐压、按键与 PA8 有效电平、电源保持时序，以及外设总线电气连接。以上均尚未完成原理图核验。

## 7. 建议的首版边界

当前范围为机器人端：接收已有上位机生成的 Python 程序、USB/蓝牙下载、程序临时驻留和解释执行、电源键长按开关机及短按运行／停止／菜单确认、用户按键与 SC7A20HTR 数据接口。另建议纳入脱机执行、基础显示、三个外接串口，以及至少一种已确定传感器和一种电机的联调。菜单具体功能待确认。

已确定本阶段不考虑上位机开发，外部 Flash 暂备用，掉电保存后续再决定。以下功能建议暂不纳入首版：多程序管理、完整 Python 标准库、第三方包安装、断点单步调试、多用户并发控制、在线遥控、无线固件升级和复杂多任务。固件首次烧录与维护方式另定，用户程序下载与固件升级分别设计。

## 8. 初步验收场景

| 编号 | 场景与预期结果 |
| --- | --- |
| A01 | 长按电源键开机后释放，PC5 保持高电平，设备稳定待机；同次按压不误触发关机或运行 |
| A02 | 长按关机，程序被终止，发送电机停止命令，PC5 拉低；分别验证电池与 USB 插入场景 |
| A03 | USB 下载、校验、运行成功，上位机可收到日志和状态 |
| A04 | 蓝牙完成连接及订阅后，同样可下载、运行和接收结果 |
| A05 | 本次上电期间完成下载后，可短按电源键运行和停止；无有效程序时提示下载。掉电保存不作为当前验收项 |
| A06 | 下载中断、损坏报文或掉电，不会导致残缺程序运行，恢复后可以重新下载 |
| A07 | 长循环、等待和外设无响应时，停止及关机操作仍能响应 |
| A08 | 三个端口按所支持设备组合工作，一个端口异常不阻塞系统 |
| A09 | Python 错误能显示原因和可获得的行号，停止电机后回到可操作状态 |
| A10 | 板载按键、显示、传感器和电池采样与实际硬件一致 |
| A11 | USB 与蓝牙同时连接时，无交叉写入或重复启动，控制权处理符合约定 |
| A12 | PC3 的状态／事件可由用户程序读取，不触发系统运行、停止或菜单操作；SC7A20HTR 数据可由用户程序读取 |
| A13 | 菜单中短按电源键只确认当前选项；长按关机后释放不产生额外短按事件。菜单完整验收在交互确定后补充 |

启动时间、停止延迟、最大程序大小、典型下载时间等量化指标，在硬件和解释器验证后补入验收标准。

## 9. 请优先修改或确认的事项

| 编号 | 待确认内容 | 当前建议或处理方式 |
| --- | --- | --- |
| Q03 | 本次上电期间下载后是否必须脱机运行 | 建议支持；掉电保存以后再决定 |
| Q04 | 电源键长按时长；菜单内容、进入／退出和选项切换方式 | 按键分工已确定，仅补充交互细节；不占用用户按键 |
| Q05 | TM1640 连接的具体显示模组和显示效果要求 | 状态提示与用户图案，尺寸待补 |
| Q06 | 三个外接端口的设备清单、协议与供电定义；电机是否超时停机 | 首先选定一种传感器和一种电机 |
| Q07 | 串口 4 的用途 | 暂预留，不占作调试口 |
| Q08 | SC7A20HTR 数据的单位、坐标方向和更新频率要求 | 已确定提供给用户编程，API 细节后续确定 |
| Q09 | 低电压阈值、USB 供电／充电行为以及 PC5 断电范围 | 根据原理图与电池规格确定 |
| Q10 | 是否需要遥控、固件在线升级或其他首版必需功能 | 当前暂列后续扩展 |

## 10. 后续推进顺序

先根据反馈修订本文件并确认剩余功能范围，再核实硬件资料与解释器资源，结合已有上位机协议及生成代码样例，形成固件技术方案、通信对接说明和 Python API 清单，之后开始机器人固件代码编写。

V0.2 修订记录：确认 MCU 为 APM32E103RET6、蓝牙为 ECB02C；确定电源键和用户按键分工；明确 SC7A20HTR 数据供用户编程；外部 Flash 改为备用，移除当前掉电保存要求；移除上位机开发范围，并同步调整使用流程和验收场景。

资料依据：用户项目说明；`IO.xlsx`；《ECB02C 蓝牙芯片技术手册 V1.0》（连接状态与控制脚见第 4 页，平台说明见第 5 页，AT 和传输相关内容见第 6–13、19–20 页）。本稿未对未提供的器件手册、原理图及实机行为作验证。
